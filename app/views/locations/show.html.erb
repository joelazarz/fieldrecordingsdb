<div class="f-container-column">

<div class="f-head">
    <%= @location.name %>
</div>

<div class="f-desc">
<%= @location.description %>
</div>
</div>

<div class="f-container-column">

<div class="f-list">
<ul>
<% @field_recordings.each do |f| %>
<li><%= link_to f.title + " - " + f.date.to_s, field_recording_path(f)%></li>
<% end %>
</ul>
</div>

</div>